---
name: pumpkin-cozy-isle
description: >-
  🏝️ 南瓜暖島美學（Pumpkin Cozy Isle）——南瓜專屬的手繪水彩／等距視角／模擬經營遊戲風格系統。
  當使用者提到「南瓜美學、暖島風格、我的美術風格、我們的設計系統、照我的風格做、手繪風、水彩風、
  等距／isometric、漂浮島風格、遊戲化介面、cozy game UI、儀表板配色、產品配色、做一個工具介面、
  UI 風格統一、幫我生素材、生圖 prompt、去背切圖」時，或任何要替南瓜做**介面／網頁工具／儀表板／
  簡報／插圖／生圖**而需要視覺一致性時，觸發此 skill。
  內含：權威色票＋CSS 變數、字體規範（含中文處理）、元件規格、生圖 prompt 模板、素材處理 SOP、
  驗收檢查表、反 AI 罐頭感清單。
---

# 🏝️ 南瓜暖島美學 Pumpkin Cozy Isle

南瓜（陳南宏）的**專屬視覺語言**。任何要代表南瓜出去的介面、工具、儀表板、插圖、生成素材，都照這份做，才會像同一個人做的。

> **一句話定義**：手繪水彩繪本的溫度 × 等距模擬經營遊戲的秩序 × 大地色系的沉穩。
> **要讓人覺得**：溫暖、有人味、聰明、想一直看下去；**不要讓人覺得**：科技冷感、AI 罐頭、企業儀表板。

## 🧠 大腦（資料）位置

- **本 skill 資料夾**＝規範本體，四份參考檔在 `references/`，色票視覺頁在 `assets/色票總覽.html`（雙擊直開）
- **活的落地範例**（風格最完整的實作）：`~/pumpkin-ai-office/`（線上 https://terry12260201.github.io/pumpkin-ai-office/ ）
  → 要看「這風格做成網頁長怎樣」，直接開這個看，不用重新想
- 相關 skill：[[pumpkin-hub]]（主控台也吃這套視覺）、[[voxel-icon]]（體素圖示，另一套風格，不要混）

## 🎨 三分鐘上手（做任何東西前先讀這段）

### 1. 色票（完整版見 `references/色票與變數.md`）

**綠是主角，紙白是舞台，木棕是配角，其餘是點綴。**

| 角色 | 色碼 | 用在哪 |
|---|---|---|
| 深森林綠 | `#2D4321` | 主色、標題、深色面板、主按鈕 |
| 極深墨綠 | `#1D3314` | 黑板風面板、最深的凹陷區 |
| 抹茶亮綠 | `#81BD49` | 進度、成功、生機、次要標題 |
| 苔草綠 | `#898B63` | 靜態標籤、次要文字底 |
| 陶土棕 | `#D4A373` | 木質高光、強調區塊 |
| 赭石棕 | `#B99A6B` | 木質家具、邊框、獎勵 |
| 紙白 | `#FCF9F0` | 主背景（模擬畫布／老紙） |
| 麥稈黃 | `#E8D9B5` | 卡片底、次要按鈕 |
| 暖灰 | `#DDDAD1` | 分隔線、未啟用、次要面板 |
| 金黃 | `#F2C94C` | 選取態、皇冠、獎勵（**少量**） |
| 磚紅 | `#C8483A` | 警示（**極少量**，一頁最多一處） |

**配色鐵則**：深綠與紙白是骨架，一頁只准一個「金黃」焦點；飽和度全部壓低，**沒有純黑 `#000` 也沒有純白 `#FFF`**。

### 2. 字體（⚠️ 中文陷阱，務必看）

- **拉丁字母／數字**：`Be Vietnam Pro`（圓潤末端、字重齊全，與手繪風契合）
- **中文**：`Be Vietnam Pro 沒有中文字符`，一定會 fallback。所以中文必須明確指定**圓體**，否則會掉進黑體、風格立刻破功。

```css
font-family: "Be Vietnam Pro", "Yuanti TC", "YouYuan", "PingFang TC",
             "Heiti TC", "Microsoft JhengHei", sans-serif;
```

- **離線單檔工具**（file:// 直開、禁外部資源）：**不得 `@import` Google Fonts**，直接靠上面的系統圓體堆疊，把 `Be Vietnam Pro` 留在第一順位即可（有裝就吃得到）。
- **字重**：標題 `700–800`，內文 `600–700`（這風格偏厚，不用 400）。標題可加字距 `letter-spacing: 1–2px`；中文按鈕文字加 `4–6px` 更有遊戲感。

### 3. 形狀與光影（風格的靈魂在這）

```css
border-radius: 8px;                        /* 基本圓角 */
border-radius: 16–22px;                    /* 卡片、面板、大按鈕 */
border: 3px solid #10240F;                 /* 墨線描邊：粗、深、明顯 */
box-shadow: 0 4px 0 rgba(30,50,25,.35);    /* ★硬邊等距投影：不模糊、不擴散 */
box-shadow: inset 0 2px 0 rgba(255,255,255,.08);  /* 深色面板的頂部微光 */
```

- **投影一律硬邊**（`blur = 0`，只給 y 位移）——這是模擬經營遊戲的積木感來源
- **禁止** `linear-gradient` 做按鈕光澤、禁止 glassmorphism、禁止霓虹發光
- 大面積可疊**紙張顆粒**（SVG feTurbulence，`opacity: .3–.35`）製造畫布質感
- 互動：`hover` 時 `translateY(-2~3px)`，`active` 時壓下去把陰影收掉——像按實體積木

### 4. 圖示

- 手繪風、**線條粗**（`stroke-width: 2–2.6`）、`stroke-linecap="round"`、末端圓潤
- 單色或雙色，不用漸層；擬物優先於抽象（用「房子」不用「首頁符號」）
- 缺圖示時：先自己畫 inline SVG（可控、零去背風險），不要硬去背點陣圖

## 🖼️ 生成素材（gpt-image-2）

風格描述咒語與完整模板見 `references/生圖prompt模板.md`。核心三段式：

1. **風格錨定**：附一張既有素材當 `image[]` 參考（比純文字描述可靠十倍）
2. **內容描述**：要畫什麼，講清楚輪廓與配色
3. **收邊咒語**：`isolated on FULLY TRANSPARENT background, no backdrop, no glow, nothing else in the image`

⚠️ **禁止叫 Codex 生圖**（沒有圖像模型）。生完一定用 PIL 驗 alpha 是否真透明。

## 🔧 素材處理（踩過的坑都在 `references/素材處理SOP.md`）

**修圖優先序，由高到低**：
1. 用**原生乾淨素材**（南瓜手上切好的最優先）
2. **重新生圖**（gpt-image-2）
3. **CSS / inline SVG 重刻**（元件類首選，零去背破綻）
4. 演算法修補（**最後手段**，會留痕跡）

**三條血淚鐵則**：
- 去背切割用 **alpha 連通域自動偵測**，不要手填座標
- `cv2.inpaint` 修大面積會**拖影**，不要用來修天空／地板
- 角色動作幀：**每幀獨立緊裁 ＋ 底部錨定 ＋ 依原始寬等比縮放**，否則切幀會跳位、大小忽大忽小

## ✅ 交付前驗收（七維度，加權 ≥90 才算過）

| 維度 | 權重 | 看什麼 |
|---|---:|---|
| 版面佈局 | 20% | 元素對位、留白節奏、視覺動線 |
| 色彩 | 15% | 只用色票內的色、飽和度壓低、焦點唯一 |
| 元件形狀 | 15% | 圓角/描邊/硬邊投影一致 |
| **可讀性** | 15% | **1280 寬全頁截圖肉眼可讀出每個字**（硬標準） |
| 角色／插圖風格 | 15% | 同一手感、同一線條粗細 |
| 質感 | 10% | 紙感顆粒、無漸層光澤、無去背毛邊 |
| 氛圍 | 10% | 溫暖有人味，不是企業儀表板 |

**字級硬底線**：面板標題 ≥18px、內文 ≥13px、大數字 ≥20px、小標籤 ≥11px。

## 🚫 反 AI 罐頭感清單（出現任何一項就是沒做好）

- ❌ 紫藍漸層、霓虹光暈、玻璃擬態、深色科技風
- ❌ 純黑純白、高飽和原色、Material/Bootstrap 預設感
- ❌ 模糊的去背邊緣、殘留的白底方塊、被拉伸變形的圖
- ❌ 素材上的假數字（12,340、Rex…）直接上線 → **一律塗掉後疊真實資料**
- ❌ 中文掉進黑體（＝忘了寫圓體 fallback）
- ❌ 一頁多個搶眼焦點、警示紅用超過一處

## 🧩 用這套風格開新東西的流程

1. 讀本檔 ＋ `references/色票與變數.md`，把 CSS 變數區塊整段複製進去
2. 需要插圖／場景／角色 → 走 `references/生圖prompt模板.md`
3. 元件優先 CSS/SVG 手刻，別急著去背
4. 做完照七維度自評 → 截圖 1280 寬檢查字級 → 不過就修
5. 若是純前端小工具：**單檔 HTML、file:// 雙擊直開、零外部資源**（南瓜的鐵則）

---

*風格擁有者：陳南宏（南瓜）｜南瓜虛擬科技｜這是南瓜自己的設計語言，不是任何現成模板*
